Тест к занятию «Прототип и конструктор объекта»

1. Какая из реализаций позволит получить Товар Телепорт бытовой VZHIH-101 по цене 9500 Q если её подставить в указанное место:
let item = {
  title: 'Телепорт бытовой VZHIH-101',
  price: 10000
 }
 // <=== строки подставляется сюда
let result = item * 0.95;
console.log(`Товар  ${item} по цене ${result} Q`);

Правильный ответ:
item.toString = function () {
  return this.title;
};
item.valueOf = function () {
  return this.price;
};

2. Что делает этот фрагмент кода var item = Object.create(obj);?
Правильный ответ: Делает объект obj прототипом объекта item

3. Что делает этот фрагмент кода var item = Object.assign({}, obj);?
Правильный ответ: Создает копию объекта obj и присваивает в переменную item

4. Как получить значение свойства с индексом 7 в массиве keys, если в переменной user объект?
const keys = Object.keys(user);
Правильный ответ: user[keys[7]]

5. Каким будет свойство city объекта location?
Object.defineProperty(location, 'city', { value: 'Ульяновск', enumerable: false });
Правильный ответ: Только для чтения, не перечисляемым, не конфигурируемым

6. Каким должен быть дискриптор обычного свойства объекта?
Правильный ответ: { enumerable: true, writeable: true, configurable: true }
