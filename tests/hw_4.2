Тест к занятию «Генераторы»

1. Что будет в переменной numbers?
function* fib() {
}
const numbers = fib();

Правильный ответ: Итератор

2. Какие строки тела генератора (номера указаны в комментарии) будут выполнены в этом коде?
function* numbers() {
   console.log('1'); // 1
    yield 42; // 2
    console.log('2'); // 3
    yield 7; // 4
    console.log('3'); // 5
}
const x = numbers();

Правильный ответ: Тело генератора не выполнится

3. Что будет выведено в консоль в этом примере:
function* numbers() {
   yield 42;
   yield 7;
   return 99;
}
console.log(...numbers());

Правильный ответ: 42, 7

4. Как задать генератор dateGenerator итератором для всех объектов созданных конструктором MyDate, если он уже создан?

Правильный ответ: MyDate.prototype[Symbol.iterator] = dateGenerator

5. Что означает запись yield* holidaysGenerator() в теле генератора dateGenerator?
Правильный ответ: Управление передается в holidaysGenerator пока он не закончится
